# requirements.txt
# Python packages required by the code *inside* the notebooks in
# this repository. Does *not* include packages necessary to
# run JupyterLab.
# Also does not currently include Text Extensions for Pandas,
# which has not yet posted a release.
cython
fastparquet
grpcio-tools
matplotlib >= 3.2.1
memoized-property
numpy
pandas >= 1.0.3
plotly
pyarrow
regex
scikit-learn
scipy
seaborn
xlrd
